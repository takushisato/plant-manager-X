export type Mail = {
  created_at: string;
  posted_member: string;
  title: string;
  message: string;
};

export type MailTable = {
  created_at: string;
  posted_member: string;
  title: string;
};
